// prisma/schema.prisma

datasource db {
  provider = "mongodb"
  url      = env("mongodb+srv://lucasmo066:5BQ1JWcvKRDuf7N8@emails.kgik85p.mongodb.net/SaharaGrill?retryWrites=true&w=majority&appName=Emails")
}

generator client {
  provider = "prisma-client-js"
}

model Email {
  id    Int    @id @default(autoincrement())
  email String @unique
  createdAt DateTime @default(now())
}
